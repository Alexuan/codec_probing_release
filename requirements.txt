# ---------------------------------------------------------------------------
# Tier 0 — reproduce the figures from the shipped caches (notebook 04). CPU-only.
# ---------------------------------------------------------------------------
numpy
scipy
scikit-learn
pandas
matplotlib
tqdm
torch>=2.0
jupyter            # to run the notebooks (nbconvert/ipykernel)

# ---------------------------------------------------------------------------
# Tier 1 — recompute codec features (notebooks 01/02/03). Needs a GPU in practice.
# ---------------------------------------------------------------------------
torchaudio>=2.0
transformers>=4.40   # provides EnCodec (facebook/encodec_24khz) and MIMI (kyutai/mimi)
soundfile
librosa
rsatoolbox           # optional: RSA (the paper reports PWCCA only)

# DAC backend (Descript Audio Codec). This also pulls `descript-audiotools`
# (import name `audiotools`) — do NOT install the unrelated PyPI `audiotools`.
descript-audio-codec

# MIMO backend (Xiaomi MiMo-Audio-Tokenizer): vendored package, install via
#   pip install -e /path/to/MiMo-Audio-Tokenizer
# plus flash-attn at runtime:
#   pip install flash-attn --no-build-isolation

# ---------------------------------------------------------------------------
# Tier 2 — build the LibriSpeech word DataFrame + word maps from scratch
# (scripts/prepare_librispeech.py). Not needed if you use the shipped pickles.
# ---------------------------------------------------------------------------
nltk               # WordNet synonyms
cmudict            # CMU Pronouncing Dictionary (phoneme sequences)
praat-textgrids    # MFA TextGrid parser (`from textgrids import TextGrid`)
jiwer              # phoneme-level edit distance for the synonym/homophone split
